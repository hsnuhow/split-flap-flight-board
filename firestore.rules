rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 所有文件只允許 server-side 存取（Cloud Functions 使用 Admin SDK 繞過此規則）
    // 前端瀏覽器無法直接讀寫 Firestore，包含 airportNames 快取集合
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
